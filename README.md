# Java_Study
Java의 정석을 바탕으로 Java에 대한 기본 교육 및 간단한 실습을 진행하는 저장소입니다.
---
### 학습 요약
#### 2026-06-28
- **학습 내용**: Java 기초 - 변수와 입출력 (`_ch02`)
- **요약**:
    - **변수의 선언과 초기화**: 데이터를 저장하기 위한 메모리 공간 확보 및 기본값 설정 방법 학습.
    - **화면 입력 (Scanner)**: `Scanner` 클래스를 사용하여 사용자로부터 문자열과 숫자를 입력받는 실습.
    - **변수의 활용**: `VarEx1`, `VarEx2` 예제를 통해 변수의 값 교환 및 출력 포맷팅 이해.
#### 2026-06-29
- **학습 내용**: Java 기초 - 연산자 (`_ch03`)
- **요약**:
    - **증감 연산자**: 전위 증감(`++i`)과 후위 증감(`i++`)의 연산 우선순위 및 값 할당 방식 학습.
    - **형변환 (Casting)**: 데이터 타입 간의 형변환(Casting) 원리와 정수/실수를 문자형(`char`)으로 변환하는 실습.
    - **산술 연산과 오버플로우**: 정수형 연산 시 오버플로우 방지를 위한 형변환 및 `Math.round()`를 이용한 실수 반올림 처리 이해.
 
#### 2026-07-01
- **학습 내용**: Java 기초 - 조건문과 반복문 (`_ch04`)
- **요약**:
    - **조건문 (if / switch)**:
      - if, if-else, if-else if 문을 사용한 조건 분기 처리 학습.
      - switch문을 통해 다중 분기를 간결하게 표현하는 방식과 break, fall-through 동작 원리 이해.
    - **반복문 (for / while / do-while)**:
      - for문의 초기화식·조건식·증감식 구조 및 중첩 for문 활용 실습.
      - while문과 do-while문의 차이 — 조건 검사 시점(선검사 vs. 후검사) 이해.
    - **분기문 (break / continue)**:
      - continue를 활용하여 특정 조건에서 나머지 반복 코드를 건너뛰는 흐름 제어 실습.
     
#### 2026-07-02 / 07-04
- **학습 내용**: Java 기초 - 배열 (`_ch05`)
- **요약**:
    - **배열 기초 (선언과 초기화)**:
      - 배열의 선언, 생성(`new`), 메모리 할당 및 기본값 초기화 방식 학습.
      - 인덱스를 통한 요소 접근과 `length` 속성을 활용한 배열 길이 확인 방법 이해.
    - **배열 활용 및 다차원 배열**:
      - 반복문을 활용한 배열 데이터 처리 (총합, 평균, 최댓값/최솟값 탐색).
      - 2차원 배열의 구조(행과 열) 이해 및 중첩 for문을 이용한 데이터 순회 실습.
    - **String 배열 및 주요 메서드**:
      - 문자열 배열의 특징 이해 및 문자열 값 비교 시 `==` 대신 `.equals()`를 사용해야 하는 이유 학습.
      - `charAt()`, `substring()` 등 String 클래스의 주요 메서드를 활용한 실전 예제(가위바위보, 영단어 퀴즈) 구현.
    - **Arrays 유틸리티 클래스**:
      - `Arrays.toString()`, `Arrays.deepToString()`을 통한 1차원 및 다차원 배열 내용 출력.
      - `Arrays.sort()`(정렬), `Arrays.copyOf()`(배열 복사), `Arrays.deepEquals()`(다차원 배열 비교) 등 편의 메서드 실습.

#### 2026-07-06
- **학습 내용**: 객체지향 프로그래밍 I - 클래스와 객체, 변수와 메서드 (`_ch06`)
- **요약**:
    - **소스파일 작성 규칙**: `public class` 명과 소스파일명의 관계 및 하나의 소스파일 내 클래스 정의 제한 규칙 실습.
    - **클래스와 객체**: 객체의 정의와 클래스 구조, `new` 연산자를 통한 인스턴스 생성 및 멤버 변수/메서드 사용법 이해.
    - **변수의 종류**: 인스턴스 변수와 클래스 변수(`static`)의 선언 위치 및 공유 메커니즘 차이 분석.
    - **메서드 정의와 호출**: 매개변수 선언, 반환 타입 및 값 반환 흐름을 학습하고, `Math.round()`를 적용한 메서드 구현 실습.
---
### 학습 계획
- **Java 기초 문법**
- **객체지향 프로그래밍**
- **자바 표준 API 활용**
- **간단한 실습 프로젝트**
